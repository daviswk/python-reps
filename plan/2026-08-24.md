# Mon Aug 24 — Week 2, Day 1: Remotes, and project 1 begins

Time: ~2.5h · A CodingBat · B git course · C flashdown kickoff.

## Block A — String-2 continues (~30 min)
1. https://codingbat.com/python/String-2 — next four in order (end_other, xyz_there, and the two after).
2. Re-type from memory → `codingbat/string_2.py`, gauntlet, commit.

## Block B — Learn Git, Chapter 9: Remote (~40 min)
3. `cd ~/gitcourse`, run chapter 9. You've been USING a remote since Day 2; now you learn what origin actually is.
4. "Why" line: what does `git pull` really do under the hood?

## Block C — Create flashdown (~60 min)
Project 1: a spaced-repetition flashcards CLI. It will eventually study YOU (cards come from your own MISTAKES/PATTERNS files).
5. On github.com: New repository → `flashdown` → public → no README (we init locally).
6. In the python-reps codespace terminal, copy the devcontainer out for reuse: `cp -r .devcontainer /tmp/dc`
7. Clone and scaffold:
   - `cd ~ && git clone https://github.com/YOURNAME/flashdown.git && cd flashdown`
   - `cp -r /tmp/dc .devcontainer`
   - `uv init --package flashdown` — note: --package on purpose. The src/ layout you stripped from python-reps is exactly right HERE, because flashdown is an installable tool. Week-0's incident becomes today's informed choice.
   - `uv add --dev pytest ruff mypy pre-commit`
8. Copy `.pre-commit-config.yaml`, the `[tool.*]` blocks of pyproject.toml, and `.github/workflows/ci.yml` from python-reps (open them side by side; type or copy carefully).
9. `uv run pre-commit install` → commit `chore: bootstrap flashdown` → push → Actions tab green.
10. In README.md, write the design sketch in 6 lines: cards have front/back/tags; decks are JSON files; `add`, `list`, `review` commands; review scheduling comes later (SM-2). Commit.
NOTE: flashdown work happens in ITS OWN codespace/clone; python-reps stays the practice home. Do NOT copy `.vscode/` — projects keep normal autocomplete.

## Done when
- [ ] String-2 at 8 · git ch 9 done · flashdown repo exists, CI green, design sketch committed

## Sources
https://codingbat.com/python/String-2 · https://www.boot.dev/courses/learn-git
