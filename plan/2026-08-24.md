# Mon Aug 24 — Week 2, Day 8: pandas, first contact

**Today in one line:** Exercism string methods · ~60 XP · penguins DataFrame · git ch 9.
**Time:** ~4h = A 60m · B 60m · C 60m · D 45m · micro 15m.

## Boot-up (3 min)
1. `cd /workspaces/python-reps && git pull` → `today`. Phone away, timer on.

---

## Block A — Exercism: string methods (60 min)

**Goal:** Little Sister's Vocab solved + re-typed. AI OFF, autocomplete OFF.

**Solve (35 min)**
1. https://exercism.org/tracks/python/exercises/little-sisters-vocab → **Start in editor**.
2. Instructions once → write → **Run Tests** → first failure → re-run. 15-min rule → hint. Green → **Submit**.

**Study (7 min)**
3. 2–3 community solutions → one cleaner move out loud → close tabs.

**Re-type from memory (13 min)**
4. `mkdir -p exercism/little_sisters_vocab` → `exercism/little_sisters_vocab/strings.py`.
5. From memory + type hints + self-checks under `if __name__ == "__main__":` → `print("ok")`.
6. `uv run python exercism/little_sisters_vocab/strings.py` → `ok`.

**Gauntlet + commit (5 min)**
7. ```
   uv run ruff format . && uv run ruff check . && uv run mypy . && uv run pytest
   ```
8. ```
   git add -A && git commit -m "feat: exercism little-sisters-vocab (string methods)" && git push
   ```
9. Stumbles → MISTAKES.md.

**☑ A done when:** submitted + re-typed + pushed.

---

## Block B — Math Academy (60 min)
1. Daily session, ~60 XP.

**☑ B done when:** ~60 XP banked.

---

## Block C — Notebook 02: meet your first dataset (60 min)

**Goal:** the first-contact ritual you'll run on every dataset for the rest of your career.

1. Colab → **New notebook** → rename to `runway02_penguins.ipynb`.
2. Cells — run each, then WRITE A MARKDOWN CELL after each answering "what did this just tell me?":
   ```python
   import seaborn as sns

   df = sns.load_dataset("penguins")
   df.head()
   ```
   ```python
   df.shape
   ```
   ```python
   df.info()
   ```
   ```python
   df.describe()
   ```
   (344 real penguins — the classic teaching dataset, and the exact shape of what your fall course will hand you.)
3. Data-dictionary markdown cell — one line per column: name · dtype · what it measures · units. (`body_mass_g` — the units are in the name. Notice that habit.)
4. **File → Save a copy in GitHub** → `daviswk/python-reps` · path `notebooks/runway02_penguins.ipynb` · message `feat: runway02 first contact`.

**☑ C done when:** notebook on GitHub with all four ritual outputs + data dictionary.

---

## Block D — Learn Git, chapter 9: Remote (45 min)
1. `cd ~/gitcourse` → course **Chapter 9** — the chapter that explains what you've been doing to GitHub all along. All lessons green.
2. "Why" line in LOG.md: *what exactly is `origin` — where does the name live, and what does it point to?*

**☑ D done when:** chapter 9 complete.

---

## Micro (15 min, hard stop)
1. Anki review (≤10 min) + cards.
2. LOG.md `## 2026-08-24 - W2 Mon` + one line.
3. `git add -A && git commit -m "chore: day 8 log" && git push`

## Bail-out plan (<90 min)
Block A + micro. Leftovers → Sunday bank.

## Done when
- [ ] Vocab submitted + re-typed + pushed
- [ ] ~60 XP
- [ ] runway02 first contact on GitHub
- [ ] Git ch 9 done · Anki · LOG pushed

## Log prompt
One sentence: what did `df.describe()` tell you about penguins that scrolling raw rows never would?

## Sources
https://exercism.org/tracks/python/exercises/little-sisters-vocab · https://pandas.pydata.org/docs/user_guide/10min.html · https://www.boot.dev/courses/learn-git
