# Thu Aug 20 — Week 1, Day 4: More strings, more items

Time: ~2–2.5h · A CodingBat · B Effective Python · C mini-build.

## Block A — String-1, next six (~35 min)
1. Continue https://codingbat.com/python/String-1 in order: first_half, without_end, combo_string, non_start, left2, right2.
2. Re-type from memory → `codingbat/string_1.py`, gauntlet, commit. (Slices everywhere today — if `s[1:-1]` still needs finger-counting, put a slice diagram in PATTERNS.md in your own notation.)

## Block B — Effective Python, Items 3–5 (~45 min)
3. Read items 3–5 with the REPL open; type and mutate every example.
4. "Why" line + at least one PATTERNS.md entry.

## Block C — Mini-build 4: expense summarizer (~50 min)
5. Create `builds/expenses.csv` with ~10 rows of `date,category,amount` (invent them).
6. `builds/expenses.py`: reads the CSV with the `csv` module, prints total per category sorted by amount descending, then a grand total. Malformed rows are skipped with a warning to stderr, never a crash.
7. Gauntlet, commit. Optional AI review; one "why" question.

## Done when
- [ ] 12/~18 String-1 done · EP items 3–5 · expenses.py survives a garbage row

## Sources
https://codingbat.com/python/String-1 · https://docs.python.org/3/library/csv.html
