# Sun Aug 30 — Week 2, Day 7: Re-type queue + read ahead

Time: ~45–60 min, light.

## Block 1 — The re-type queue (~30 min)
1. Five hardest problems of the week from MISTAKES.md → re-type from memory into `drills/retype_2026-08-30.py`, fresh asserts, no peeking until stuck.
2. One flashdown function too: re-type `save_deck`/`load_deck` from memory in a scratch file, then diff against the real one. Projects deserve encoding, not just problems.
3. Commit.

## Block 2 — Read ahead (~15 min)
4. `today --list 7` (Week 3 files should exist if yesterday's batch request went out — if not, send it now, one line).
5. Read Monday fully. Push, clean, stop, rest.

## Done when
- [ ] Re-types done (incl. one project function) · Week 3 visible in `today --list`
