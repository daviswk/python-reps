# Sun Aug 23 — Week 1, Day 7: Re-solve, read, rest

**Today in one line:** re-solve queue from memory · safety paper #1 · ~60 XP · preview Week 2 · rest.
**Time:** ~4.5h scheduled. The rest of your 6h is **banked recovery** — catch-up only, else free.

## Boot-up (3 min)
1. `cd /workspaces/python-reps && git pull` → `today`.

---

## Re-solve queue (60 min)

**Goal:** this week's solutions, reproduced cold. Recall is the skill; today measures it.

1. NO peeking at your old code, the site, or community solutions. Fresh files:
   ```
   timed/resolve_ghost_gobble.py
   timed/resolve_black_jack.py
   timed/resolve_meltdown.py
   ```
2. Re-solve each from memory (~15 min each). Include the `if __name__ == "__main__":` self-checks from memory too.
3. Run each: `uv run python timed/resolve_ghost_gobble.py` etc.
4. Now compare: open the original next to the re-solve. Every real difference → one line in MISTAKES.md (forgot it) or PATTERNS.md (create it if absent — improved it).
5. Gauntlet → `git add -A && git commit -m "feat: sunday re-solve queue W1" && git push`

**☑ done when:** 3 re-solves pushed + diffs harvested into MISTAKES/PATTERNS.

---

## Weekly safety paper #1 (60 min)

6. Read: Anthropic, **Core Views on AI Safety** — https://www.anthropic.com/news/core-views-on-ai-safety (essay, not math — read all of it).
7. Create `papers/notes.md` (make the `papers/` folder). Paste this template, then fill it:
   ```
   # Paper notes

   ## 2026-08-23 — Core Views on AI Safety (Anthropic)
   claim:
   why it matters:
   my question:
   one connection:
   next paper: Concrete Problems in AI Safety — https://arxiv.org/abs/1606.06565
   ```
8. `git add -A && git commit -m "docs: paper note 1 — core views" && git push`
   (This shelf becomes research-statement fuel when the fellowship window opens.)

**☑ done when:** papers/notes.md pushed with note #1.

---

## Math Academy (60 min)
9. Daily session, ~60 XP. Seven days, seven sessions — first full week.

## Polish / get-ahead (45 min — pick exactly ONE)
10. Either: one more practice exercise · OR runway01 touch-ups · OR peek at next week's dataset in a scratch Colab: `import seaborn as sns; sns.load_dataset("penguins").head()` — look, don't work.

## Preview (30 min)
11. `today --list 7` then `today --next` — read Monday's file fully.
12. LOG.md `## 2026-08-23 - W1 Sun` + one sentence of intent for Week 2.
13. `git add -A && git commit -m "chore: day 7 log" && git push`

## Micro
14. Anki review (≤10 min).

## Banked recovery (~1.5h)
Anything that slipped this week gets caught up here. Nothing slipped? **Actually rest.** Week 2 is a build week.

## Done when
- [ ] 3 re-solves pushed · MISTAKES/PATTERNS updated
- [ ] Paper note #1 pushed
- [ ] ~60 XP · Week 2 previewed · Anki done

## Log prompt
Week 1 complete — all seven days. One sentence: the clearest difference between how Monday-you and Sunday-you read `rng.normal(0, 1, 10_000)`.

## Sources
https://www.anthropic.com/news/core-views-on-ai-safety · https://arxiv.org/abs/1606.06565
