# Tue Aug 25 — Week 2, Day 9: Selecting truth from tables

**Today in one line:** Exercism lists · ~60 XP · loc/iloc + the missing-data decision · git ch 10.
**Time:** ~4h = A 60m · B 60m · C 60m · D 45m · micro 15m.

## Boot-up (3 min)
1. `cd /workspaces/python-reps && git pull` → `today`. Phone away, timer on.

---

## Block A — Exercism: lists (60 min)

**Goal:** Elyse's Enchantments solved + re-typed. AI OFF, autocomplete OFF.

**Solve (35 min)**
1. https://exercism.org/tracks/python/exercises/elyses-enchantments → **Start in editor**.
2. Instructions once → write → **Run Tests** → first failure → re-run. 15-min rule → hint. Green → **Submit**. (Trap of the day: which list methods mutate in place and return `None`.)

**Study (7 min)**
3. 2–3 community solutions → one cleaner move → close tabs.

**Re-type from memory (13 min)**
4. `mkdir -p exercism/elyses_enchantments` → `exercism/elyses_enchantments/lists.py`.
5. From memory + hints + self-checks → `uv run python exercism/elyses_enchantments/lists.py` → `ok`.

**Gauntlet + commit (5 min)**
6. ```
   uv run ruff format . && uv run ruff check . && uv run mypy . && uv run pytest
   ```
7. ```
   git add -A && git commit -m "feat: exercism elyses-enchantments (lists)" && git push
   ```
8. Stumbles → MISTAKES.md.

**☑ A done when:** submitted + re-typed + pushed.

---

## Block B — Math Academy (60 min)
1. Daily session, ~60 XP.

**☑ B done when:** ~60 XP banked.

---

## Block C — Notebook 02: slicing + the missingness decision (60 min)

1. Colab → open `runway02_penguins.ipynb` from GitHub. Markdown cell: `## Selecting`.
2. Selection reps — one cell each, predict the output BEFORE running:
   ```python
   df["species"]
   ```
   ```python
   df[["species", "body_mass_g"]]
   ```
   ```python
   df.loc[df["island"] == "Dream"]
   ```
   ```python
   df.iloc[:5, :3]
   ```
3. Markdown cell, one line, your words: loc vs iloc.
4. Counting cells:
   ```python
   df["species"].value_counts()
   ```
   — repeat for `island` and `sex`.
5. Markdown cell: `## The missing-data decision`. Then:
   ```python
   df.isna().sum()
   ```
6. There ARE missing penguins. Write the decision in markdown BEFORE coding it: drop or keep? How many rows die (`df.dropna().shape` vs `df.shape`)? Which analyses could that bias? Then:
   ```python
   clean = df.dropna().copy()   # never overwrite the raw df
   clean.shape
   ```
7. Save a copy in GitHub, same path, message `feat: runway02 selection + missingness decision`.

**☑ C done when:** selection reps + written decision + `clean` on GitHub.

---

## Block D — Learn Git, chapter 10: GitHub (45 min)
1. `cd ~/gitcourse` → course **Chapter 10**, all lessons green.
2. "Why" line in LOG.md: *git vs GitHub — one sentence each, no overlap allowed.*

**☑ D done when:** chapter 10 complete.

---

## Micro (15 min, hard stop)
1. Anki review (≤10 min) + cards.
2. LOG.md `## 2026-08-25 - W2 Tue` + one line.
3. `git add -A && git commit -m "chore: day 9 log" && git push`

## Bail-out plan (<90 min)
Block A + micro. Leftovers → Sunday bank.

## Done when
- [ ] Lists submitted + re-typed + pushed
- [ ] ~60 XP
- [ ] Missingness decision written + committed
- [ ] Git ch 10 done · Anki · LOG pushed

## Log prompt
Your dropna decision, one sentence, as you'd defend it to a professor. (Your fall course grades exactly this judgment.)

## Sources
https://exercism.org/tracks/python/exercises/elyses-enchantments · https://pandas.pydata.org/docs/user_guide/indexing.html · https://www.boot.dev/courses/learn-git
