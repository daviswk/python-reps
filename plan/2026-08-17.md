# Mon Aug 17 — Week 1, Day 1: Foundations begin

Time: ~2–2.5h · Blocks: A CodingBat · B git course · C mini-build.

## Block A — Warmup-2, first five (~35 min)
1. https://codingbat.com/python/Warmup-2 — solve in order: string_times, front_times, string_bits, string_splosion, last2. (This module introduces loops and slices — expect it to feel one notch harder than Warmup-1. Good.)
2. Re-type from memory into `codingbat/warmup_2.py` with type hints + asserts (same shape as warmup_1.py).
3. Gauntlet green, commit `feat: codingbat warmup-2 first 5`. Stumbles → MISTAKES.md.

## Block B — Learn Git, Chapter 4: Config (~40 min)
4. `cd ~/gitcourse`, run chapter 4 with browser + terminal side by side.
5. "Why" line: why does git have three levels of config instead of one?

## Block C — Mini-build 1: unit converter (~50 min)
6. In python-reps, create `builds/convert.py` (make the `builds/` folder; right-click it → New File — the path vaccine).
7. Build a CLI: `uv run python builds/convert.py 100 f-to-c` prints the conversion. Use argparse with two positional args (value, mode); support f-to-c, c-to-f, mi-to-km, km-to-mi. Bad mode → clear message, exit code 1, no traceback.
8. No AI writes any line (Block C = you author, AI may review after). When done, run it five ways including two wrong ways.
9. Gauntlet, commit `feat: convert mini-build`. Optional: paste the finished file to Claude and ask "review this — what's unidiomatic?" Log one takeaway.

## Done when
- [ ] 5 Warmup-2 solved + re-typed · git ch 4 done · convert.py works and fails politely

## Log prompt
One sentence: which felt better today, the site's instant feedback or the from-memory re-type — and why?

## Sources
https://codingbat.com/python/Warmup-2 · https://www.boot.dev/courses/learn-git
