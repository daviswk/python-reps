# Mon Aug 17 — Week 1, Day 1: Runway ignition

**Today in one line:** first Exercism rep · math placement diagnostic · first NumPy notebook · git ch 4.
**Time:** ~4h = A 60m · B 60m · C 60m · D 45m · micro 15m. Take a 5–10 min break between blocks — off screen.

## Boot-up (3 min, before any block)

1. Open the codespace. Terminal: `cd /workspaces/python-reps && git pull`
2. Type `today` — you're reading the right file.
3. Phone in another room or on Do Not Disturb. Start when the timer starts, not before.

---

## Block A — Exercism: booleans (60 min — set a 60-min timer now)

**Goal:** Ghost Gobble solved on the site, then re-typed from memory into the repo.
**AI OFF. Autocomplete OFF.** (Repo settings enforce this in the editor; the browser is on your honor.)

**Setup (3 min)**
1. Browser: https://exercism.org/tracks/python/exercises/ghost-gobble-arcade-game
2. Click **Start in editor** (the in-browser editor — no downloads today).

**Solve (30 min)**
3. Read the whole instructions panel once, top to bottom, before typing anything.
4. Write the functions. Run tests with the **Run Tests** button as often as you like.
5. Red tests: read only the FIRST failure, fix it, re-run. One failure at a time.
6. Stuck 15 min with zero progress? Open a hint. A used hint is data, not failure — it goes in MISTAKES.md later.
7. All green → **Submit**.

**Study the field (7 min)**
8. Open **Community Solutions**. Read 2–3. Say out loud one thing someone did cleaner than you.
9. Close ALL solution tabs. Closed means closed.

**Re-type from memory (15 min)**
10. Terminal:
    ```
    mkdir -p exercism/ghost_gobble
    ```
11. Create `exercism/ghost_gobble/ghost_gobble.py` in the editor. Re-type your solution FROM MEMORY — no peeking at the site. Type hints on every function (mypy runs strict). At the bottom, quick self-checks:
    ```python
    if __name__ == "__main__":
        assert game_loop_over(0) is True  # use your real functions/cases
        print("ok")
    ```
12. Run it: `uv run python exercism/ghost_gobble/ghost_gobble.py` → prints `ok`.

**Gauntlet + commit (5 min)**
13. ```
    uv run ruff format . && uv run ruff check . && uv run mypy . && uv run pytest
    ```
    Fix what it flags (ruff's message tells you the line; re-run until clean).
14. ```
    git add -A && git commit -m "feat: exercism ghost-gobble (booleans)" && git push
    ```
15. Anything that tripped you today → one line in MISTAKES.md now, format: *pattern · wrong turn · one-line insight*.

**☑ Block A done when:** site shows submitted + repo file exists with hints and asserts + pushed.

---

## Block B — Math Academy: the placement diagnostic (60 min)

**Goal:** account exists, diagnostic started (finishing it today is a bonus, not the goal).

1. Browser: https://www.mathacademy.com → **Sign up**. (Paid, ~$49/mo. It is the math spine of the next 36 weeks — the adaptive engine is what you're buying. Decide once, here: subscribe.)
2. In onboarding, your target course is **Mathematical Foundations** (the adaptive on-ramp; it routes you through any algebra/precalc gaps automatically — Linear Algebra and Probability & Statistics unlock after it).
3. Start the **diagnostic**. Rules, non-negotiable:
   - No calculator unless it offers one. No looking anything up. No scratch-paper Googling.
   - Wrong answers are GOOD — they place the ladder at your true rung. Zero-to-Hero needs an honest zero.
4. Work until the diagnostic ends or the hour does, whichever comes first. It saves progress; tomorrow's Block B resumes it if needed.

**☑ Block B done when:** account exists + diagnostic in progress or finished.

---

## Block C — Notebook 01: NumPy first contact (60 min)

**Goal:** a notebook exists in your repo, created in Colab, with the vectorization punchline.

**Setup (5 min)**
1. Browser: https://colab.research.google.com → sign in with Google → **New notebook**.
2. Click the title (top-left, says `Untitled0.ipynb`) → rename to `runway01_randomness.ipynb`.

**Reps (40 min)** — type these, run each cell with Shift+Enter, and READ every output:
3. Cell 1:
    ```python
    import numpy as np

    a = np.array([1, 2, 3, 4, 5])
    a, a.shape, a.dtype
    ```
4. Cell 2 — arithmetic hits the whole array at once (no loop!):
    ```python
    a * 10, a + a, a**2
    ```
5. Cell 3 — the punchline, Python loop vs NumPy. Two cells, time them both:
    ```python
    %%timeit
    total = 0
    for i in range(10_000_000):
        total += i
    ```
    ```python
    %%timeit
    np.arange(10_000_000).sum()
    ```
6. Markdown cell (click **+ Text**): in your own words, 2–3 sentences — why is the second one ~50–100× faster? (Hint: where does each loop actually run — Python or C?)
7. Optional if time remains: `np.arange(0, 1, 0.1)`, `np.linspace(0, 1, 11)`, `a[a > 2]` — one cell each, guess the output BEFORE running.

**Ship it (10 min)**
8. **File → Save a copy in GitHub**. First time: authorize GitHub when asked.
9. Repository: `daviswk/python-reps` · Branch: `main` · File path: `notebooks/runway01_randomness.ipynb` · commit message: `feat: runway01 numpy basics` → **OK**.
10. Verify: open github.com/daviswk/python-reps — the `notebooks/` folder exists with your file. (Back in the codespace, `git pull` grabs it.)

**☑ Block C done when:** the notebook is visible on GitHub with the %%timeit comparison + your markdown explanation.

---

## Block D — Learn Git, chapter 4: Config (45 min)

1. Terminal: `cd ~/gitcourse` (it exists from chapters 1–3). Check the CLI answers: `bootdev --help` (if missing: `go install github.com/bootdotdev/bootdev@latest`).
2. Browser: https://www.boot.dev/courses/learn-git → **Chapter 4: Config**.
3. Side-by-side: site on the left, terminal on the right. Each lesson gives you the exact `bootdev run ...` command — paste, run, submit.
4. Finish the chapter (all lessons green). 8 chapters left after today; done Aug 26.
5. "Why" line in LOG.md: *why does git have three levels of config instead of one?*

**☑ Block D done when:** chapter 4 shows complete on the course page.

---

## Micro (15 min, hard stop)

1. Anki setup (one-time): create a free account at https://ankiweb.net. Best home for it: your PHONE — install AnkiDroid (free) or AnkiMobile (iOS) and sign in, so reviews happen in dead moments. Create deck: `reps`.
2. Add 3 cards from MISTAKES.md (type: Basic. Front = the situation, e.g. "pre-commit fails after fixing files — what did I forget?" · Back = the insight, e.g. "re-stage: git add again before committing").
3. LOG.md, new entry:
    ```
    ## 2026-08-17 - W1 Mon (v3 D1)
    <one honest line about today>
    ```
4. Final: `git add -A && git commit -m "chore: day 1 log" && git push`

---

## Bail-out plan (life happened, you have <90 min)

Do Block A + micro. That's the day, guilt-free — the streak lives. Push B/C/D leftovers to Sunday's banked recovery, note it in LOG.

## Done when
- [ ] Ghost Gobble: submitted + re-typed + pushed
- [ ] Math Academy: account + diagnostic underway
- [ ] runway01 notebook on GitHub with timing comparison
- [ ] Git ch 4 complete
- [ ] Anki deck `reps` exists with 3 cards · LOG entry pushed

## Log prompt
One sentence: what did the placement diagnostic tell you about where your math actually is (vs where you remembered it being)?

## Sources
https://exercism.org/tracks/python/exercises/ghost-gobble-arcade-game · https://www.mathacademy.com · https://colab.research.google.com · https://www.boot.dev/courses/learn-git · https://ankiweb.net
