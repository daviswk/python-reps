# Tue Aug 18 — Week 1, Day 2: Finish Warmup-2, meet Effective Python

Time: ~2–2.5h · A CodingBat · B Effective Python · C mini-build.

## Block A — Warmup-2, finished (~35 min)
1. Solve the rest of https://codingbat.com/python/Warmup-2 in order: array_count9, array_front9, array123, string_match.
2. Re-type from memory → `codingbat/warmup_2.py`, hints + asserts, gauntlet, commit `feat: codingbat warmup-2 complete`. Module 2 of 8 done.

## Block B — Effective Python, Items 1–2 (~45 min)
3. Read Item 1 (know your Python version) and Item 2 (PEP 8) with the REPL open. For every code example: type it, run it, then mutate it — "what happens if I…".
4. These two are gentle on purpose; the habit being installed is read-type-mutate, not speed.
5. "Why" line in LOG.md, and one card-worthy idea into PATTERNS.md (create the file: pattern · where seen · when to reach for it).

## Block C — Mini-build 2: file renamer (~50 min)
6. `builds/rename.py`: takes a folder path and a suffix, renames every file in it to include the suffix before the extension — BUT default is a dry run that only prints what it would do; a `--force` flag actually renames. pathlib only, no os.path.
7. Test it on a scratch folder you create with `mkdir /tmp/renametest && touch /tmp/renametest/{a,b,c}.txt`.
8. Gauntlet, commit. Optional AI review of the finished file — ask one "why" question about its feedback.

## Done when
- [ ] Warmup-2 complete on-site + in-repo · EP items 1–2 typed-through · dry-run renamer safe by default

## Sources
https://codingbat.com/python/Warmup-2 · Effective Python 3e, items 1–2
