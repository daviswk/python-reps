# Sat Aug 22 — Week 1, Day 6: First long-block Saturday

Time: ~6h · Long notebook stretch · adversarial rep #1 · problems · math · window check · review

## Long block — Notebook 01 stretch: the distribution zoo (~2h)
1. New final section in `runway01_randomness.ipynb`: 2×2 subplot grid — uniform, normal, binomial (`rng.binomial(20, 0.3, 10_000)`), Poisson (`rng.poisson(3, 10_000)`).
2. Under each: one markdown line naming a real policy quantity it models (e.g. Poisson → counts of rare events per month).
3. Stretch rep: overlay the normal's histogram with a second normal at different σ — one plot, labeled legend. What does σ *look like*?
4. Save to GitHub, commit `feat: runway01 distribution zoo`.

## Adversarial rep #1 (~45 min) — AI writes, you demolish
5. Mode 3 of the AI policy: ask Claude for `def summarize(nums: list[float]) -> dict` returning mean/median/std — and ask it to *plant two subtle bugs*.
6. Four-lens teardown, line by line: correctness · edge cases (empty list? one item? NaN?) · readability · how-could-this-fail-in-production.
7. Findings → REVIEWS.md (new file if absent). Then fix the function yourself, add asserts that would have caught both bugs, commit `feat: adversarial rep 1 — summarize()`.

## Problems (~60 min)
8. Two practice exercises from the tree, your pick (good candidates: Pangram, Reverse String, Isogram). Site → re-type → commit.

## Math Academy (~60 min)
9. Daily session, ~60 XP.

## Fellowship window check (~15 min — monthly ritual, first Saturdays)
10. Check https://www.anthropic.com/careers (search "Fellows") for an open application window. One LOG line: open / not open / dates if listed. That's the whole task — no rabbit hole.

## Saturday review (~45 min)
11. Read the week's LOG entries. Two sentences at the bottom: what worked, what dragged.
12. MISTAKES.md → 3 new Anki cards.
13. `today --list 7` — skim the coming week. Move nothing yet; just see it.

## Done when
- [ ] Zoo committed · REVIEWS.md has teardown #1 · 2 practice exercises · ~60 XP · window check logged · review written

## Log prompt
The adversarial rep: did you find both planted bugs — and did you find any Claude didn't plant?

## Sources
https://colab.research.google.com · https://exercism.org/tracks/python · https://www.anthropic.com/careers
