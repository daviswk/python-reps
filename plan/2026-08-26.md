# Wed Aug 26 — Week 2, Day 3: Lists mid, GitHub chapter, add command

Time: ~2.5h · A CodingBat · B git course · C flashdown.

## Block A — List-1 continues (~35 min)
1. https://codingbat.com/python/List-1 — next five in order: common_end, sum3, rotate_left3, reverse3, max_end3.
2. Re-type from memory → `codingbat/list_1.py`, gauntlet, commit.

## Block B — Learn Git, Chapter 10: GitHub (~40 min)
3. `cd ~/gitcourse`, run chapter 10 — forks, PRs, the collaboration layer on top of git itself. Connect it to your own self-PR convention.
4. "Why" line: what does GitHub add that git alone doesn't have?

## Block C — flashdown: add (~55 min)
5. `src/flashdown/cli.py`: argparse with subcommands; implement `add` — `uv run flashdown add "front text" "back text" --tag python` appends a Card to the deck file (create file if missing; default path `~/.flashdown/deck.json`, overridable with `--deck`).
6. Wire the console entry: confirm `[project.scripts]` from `uv init --package` points at your main; adjust so `uv run flashdown add ...` works.
7. Failure paths: corrupt JSON → clear message + exit 1; missing directory → create it.
8. Test: `tests/test_cli_add.py` using tmp_path deck. Gauntlet, commit on a BRANCH → open your first flashdown self-PR on github.com → fill the "how could this fail / be exploited?" section (PRINCIPLES.md §2: two honest sentences — think path handling and malformed JSON) → review your own diff line-by-line → merge.

## Done when
- [ ] List-1 at 8 · git ch 10 done · `flashdown add` works, self-PR merged with adversarial section

## Sources
https://codingbat.com/python/List-1 · https://www.boot.dev/courses/learn-git
