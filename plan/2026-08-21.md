# Fri Aug 21 — Week 1, Day 5: Ship notebook 01

**Today in one line:** Exercism strings · ~60 XP · runway01 goes v1.0 · git ch 8.
**Time:** ~4h = A 60m · B 60m · C 60m · D 45m · micro 15m.

## Boot-up (3 min)
1. `cd /workspaces/python-reps && git pull` → `today`. Phone away, timer on.

---

## Block A — Exercism: strings (60 min)

**Goal:** Little Sister's Essay solved + re-typed. AI OFF, autocomplete OFF.

**Solve (35 min)**
1. https://exercism.org/tracks/python/exercises/little-sisters-essay → **Start in editor**.
2. Instructions once → write → **Run Tests** → first failure → re-run. 15-min rule → hint.
3. Green → **Submit**. (Today's trap: strings are immutable — every method returns a NEW string.)

**Study (7 min)**
4. 2–3 community solutions → one cleaner move out loud → close tabs.

**Re-type from memory (13 min)**
5. `mkdir -p exercism/little_sisters_essay` → `exercism/little_sisters_essay/essay.py`.
6. From memory + hints + self-checks under `if __name__ == "__main__":`, ending `print("ok")`.
7. `uv run python exercism/little_sisters_essay/essay.py` → `ok`.

**Gauntlet + commit (5 min)**
8. ```
   uv run ruff format . && uv run ruff check . && uv run mypy . && uv run pytest
   ```
9. ```
   git add -A && git commit -m "feat: exercism little-sisters-essay (strings)" && git push
   ```
10. Stumbles → MISTAKES.md.

**☑ A done when:** submitted + re-typed + pushed.

---

## Block B — Math Academy (60 min)
1. Daily session, ~60 XP. That's five weekday sessions banked — the streak is real.

**☑ B done when:** ~60 XP banked.

---

## Block C — runway01 → v1.0 (60 min)

**Goal:** a stranger could read this notebook and learn something. That's the portfolio bar.

1. Colab → open `runway01_randomness.ipynb`.
2. Top of notebook, first cell = markdown title block:
   ```
   # Runway 01 — From numbers to randomness
   NumPy basics → coin flips → distributions → the law of large numbers.
   Part of a 36-week self-study plan; notebook 1 of 6 in the stats runway.
   ```
3. Check every section has a `##` markdown heading: NumPy basics · Coin flips · Distributions · Law of large numbers.
4. Check every plot: title, x-label, y-label. Fix the ones you skipped.
5. The stranger test: **Runtime → Run all**. Read top to bottom as someone who wasn't there. Anywhere you'd have to explain out loud → fix the markdown.
6. Save a copy in GitHub, same path, message `feat: runway01 v1.0 — numbers to randomness`.
7. In the codespace: `git pull` — if pre-commit ever auto-fixes the .ipynb on a later commit, just `git add -A` and commit again.
8. Look at what exists: **your first shipped artifact of v3, on Day 5.** Say it out loud.

**☑ C done when:** Run-all works clean + v1.0 commit is on GitHub.

---

## Block D — Learn Git, chapter 8: Reset (45 min)
1. `cd ~/gitcourse` → course **Chapter 8**, all lessons green.
2. "Why" line in LOG.md: *soft vs hard reset — which one would have saved you during the Aug 11 pre-commit fight, and why?*

**☑ D done when:** chapter 8 complete.

---

## Micro (15 min, hard stop)
1. Anki review (≤10 min) + cards.
2. LOG.md `## 2026-08-21 - W1 Fri` + one line.
3. `git add -A && git commit -m "chore: day 5 log" && git push`

## Bail-out plan (<90 min)
Block C (ship the notebook) + micro today — shipping beats reps on a ship day. Leftovers → tomorrow's long block.

## Done when
- [ ] Essay submitted + re-typed + pushed
- [ ] ~60 XP
- [ ] **runway01 v1.0 shipped**
- [ ] Git ch 8 done · Anki · LOG pushed

## Log prompt
Week 1 weekdays done. One sentence: which block do you open eagerly, and which do you drag yourself into? (This calibrates Week 3+.)

## Sources
https://exercism.org/tracks/python/exercises/little-sisters-essay · https://www.mathacademy.com · https://www.boot.dev/courses/learn-git
